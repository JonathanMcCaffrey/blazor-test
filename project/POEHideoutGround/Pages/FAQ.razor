@page "/faq"
@inject HttpClient Http
@((MarkupString)markdown)

@code {
    string markdown;

    protected override async Task OnInitializedAsync()
    {
        markdown = Markdig.Markdown.ToHtml(await Http.GetStringAsync("markdown/faq.md"));
    }
}